services:
  - type: web
    name: neurocognitive-ai
    runtime: python
    runtimeVersion: "3.12"
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
